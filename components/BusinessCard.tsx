import Link from 'next/link';import type{Business}from'@/lib/types'
export function BusinessCard({business}:{business:Business}){return <article className="business-card"><div className="business-icon">{business.abbr||business.name.slice(0,2).toUpperCase()}</div><div className="business-main"><div className="badges">{business.verified&&<span className="badge verified">Verified</span>}{business.featured&&<span className="badge sponsored">Featured</span>}{business.claimed&&<span className="badge neutral">Claimed</span>}</div><h3><Link href={`/business/${business.slug}`}>{business.name}</Link></h3>{business.address_text&&<p>{business.address_text}</p>}<p className="muted">{business.description||'View this local business profile for contact details, services and listing information.'}</p><div className="card-actions"><Link className="btn btn-primary" href={`/business/${business.slug}`}>View Profile</Link>{business.phone&&<a className="btn btn-light" href={`tel:${business.phone}`}>Call</a>}</div></div></article>}
