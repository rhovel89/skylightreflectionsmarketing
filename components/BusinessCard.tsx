import Link from 'next/link';import type{Business}from'@/lib/types'
type MatchedLocation={address_text?:string|null;city?:string|null;state?:string|null;postal_code?:string|null;phone?:string|null}
type PublicBusiness=Business&{matched_location?:MatchedLocation;is_sponsored?:boolean}
export function BusinessCard({business}:{business:Business}){const b=business as PublicBusiness;const matched=b.matched_location;const address=matched?.address_text||b.address_text;const phone=matched?.phone||b.phone;return <article className="business-card"><div className="business-icon">{b.abbr||b.name.slice(0,2).toUpperCase()}</div><div className="business-main"><div className="badges">{b.verified&&<span className="badge verified">Verified</span>}{b.is_sponsored&&<span className="badge sponsored">Sponsored</span>}{b.claimed&&<span className="badge neutral">Claimed</span>}</div><h3><Link href={`/business/${b.slug}`}>{b.name}</Link></h3>{address&&<p className="small">{address}</p>}<p className="muted">{b.description||'View this local business profile for contact details, services and listing information.'}</p><div className="card-actions"><Link className="btn btn-primary" href={`/business/${b.slug}`}>View Profile</Link>{phone&&<a className="btn btn-light" href={`tel:${phone}`}>Call</a>}{b.website&&<a className="btn btn-light" href={b.website} target="_blank" rel="noopener noreferrer">Website</a>}</div></div></article>}
