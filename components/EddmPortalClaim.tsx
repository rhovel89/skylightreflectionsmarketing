'use client'
import{useState}from'react'
export function EddmPortalClaim({token}:{token:string}){const[msg,setMsg]=useState(''),[busy,setBusy]=useState(false);const claim=async()=>{setBusy(true);setMsg('');try{const r=await fetch('/api/account/eddm',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({action:'claim',token})}),b=await r.json().catch(()=>({}));if(!r.ok)throw new Error(String(b.error||'Unable to connect portal.'));setMsg('Portal connected. Opening your EDDM workspace…');window.location.href='/account/eddm?connected=1'}catch(e:any){setMsg(e.message)}finally{setBusy(false)}};return <div className="card" style={{maxWidth:680,margin:'0 auto',padding:28}}><div className="kpi">Secure EDDM Access</div><h1>Connect your Skylight campaign</h1><p className="muted">This invitation is tied to the email address that received it. Sign in or create your Local Pros account using that same email, then connect the campaign.</p>{msg?<div className="notice">{msg}</div>:null}<button className="btn btn-primary" disabled={busy} onClick={()=>void claim()}>{busy?'Connecting…':'Connect My EDDM Campaign'}</button><p className="small muted" style={{marginTop:16}}>Connecting this portal does not affect Local Pros verification, organic ranking or Sponsored placement.</p></div>}
