import{LAUNCH_CITIES}from'@/lib/constants';import type{Category,Location}from'@/lib/types'
type SearchDefaults={category?:string;city?:string;q?:string}
export function SearchForm({categories=[],locations=[],defaults={}}:{categories?:Category[];locations?:Location[];defaults?:SearchDefaults}){const cityOptions=locations.length?locations.map(l=>({key:l.id,value:l.slug,label:l.name})):LAUNCH_CITIES.map(c=>({key:c,value:c.toLowerCase(),label:c}));return <form className="searchbox" action="/search" method="get"><select name="category" defaultValue={defaults.category??''} aria-label="Business category"><option value="">What do you need?</option>{categories.map(c=><option key={c.id} value={c.slug}>{c.name}</option>)}</select><select name="city" defaultValue={defaults.city??''} aria-label="City"><option value="">Choose a city</option>{cityOptions.map(c=><option key={c.key} value={c.value}>{c.label}</option>)}</select><input name="q" defaultValue={defaults.q??''} placeholder="Business name or keyword" aria-label="Business name or keyword"/><button className="btn btn-primary" type="submit">Search Local Pros</button></form>}
