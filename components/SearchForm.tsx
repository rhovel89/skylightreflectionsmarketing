import {LAUNCH_CITIES}from'@/lib/constants';import type{Category}from'@/lib/types'
export function SearchForm({categories=[]}:{categories?:Category[]}){return <form className="searchbox" action="/search" method="get"><select name="category" defaultValue=""><option value="">What do you need?</option>{categories.map(c=><option key={c.id} value={c.slug}>{c.name}</option>)}</select><select name="city" defaultValue=""><option value="">Choose a city</option>{LAUNCH_CITIES.map(c=><option key={c} value={c.toLowerCase()}>{c}</option>)}</select><input name="q" placeholder="Business name or keyword"/><button className="btn btn-cyan" type="submit">Search Local Pros</button></form>}
