'use client'
import{useState}from'react';import type{AdminEntityConfig}from'@/lib/admin'
export function AdminEntityEditor({section,cfg,rows}:{section:string;cfg:AdminEntityConfig;rows:Record<string,unknown>[]}){const[data,setData]=useState(rows);const[status,setStatus]=useState('');async function save(id:string,changes:Record<string,unknown>){setStatus('Saving…');const r=await fetch('/api/admin/entity',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({section,id,changes})});const body=await r.json();setStatus(r.ok?'Saved.':body.error||'Save failed.')}return <div><p className="muted">{cfg.description}</p><div className="admin-table-wrap"><table className="admin-table"><thead><tr>{Object.keys(data[0]??{}).map(k=><th key={k}>{k}</th>)}{!cfg.readOnly&&<th>Action</th>}</tr></thead><tbody>{data.map((row,idx)=><EditableRow key={String(row.id??idx)} row={row} editable={cfg.editable} readOnly={cfg.readOnly} onSave={(changes)=>save(String(row.id),changes)} onChange={(k,v)=>setData(prev=>prev.map((x,i)=>i===idx?{...x,[k]:v}:x))}/>)}</tbody></table></div><p className="small muted">{status}</p>{!data.length&&<div className="empty">No records in this section yet.</div>}</div>}
function EditableRow({row,editable,readOnly,onSave,onChange}:{row:Record<string,unknown>;editable:string[];readOnly?:boolean;onSave:(changes:Record<string,unknown>)=>void;onChange:(k:string,v:unknown)=>void}){const[dirty,setDirty]=useState<Record<string,unknown>>({});const set=(k:string,v:unknown)=>{onChange(k,v);setDirty(d=>({...d,[k]:v}))};return <tr>{Object.entries(row).map(([k,v])=><td key={k}>{editable.includes(k)?<Editor value={v} onChange={x=>set(k,x)}/>:<span>{typeof v==='object'?JSON.stringify(v):String(v??'')}</span>}</td>)}{!readOnly&&<td><button className="btn btn-small" disabled={!Object.keys(dirty).length} onClick={()=>{onSave(dirty);setDirty({})}}>Save</button></td>}</tr>}
function Editor({value,onChange}:{value:unknown;onChange:(v:unknown)=>void}){if(typeof value==='boolean')return <input type="checkbox" checked={value} onChange={e=>onChange(e.target.checked)}/>;if(typeof value==='number')return <input className="table-input" type="number" value={value} onChange={e=>onChange(Number(e.target.value))}/>;if(value!==null&&typeof value==='object')return <textarea className="table-input" value={JSON.stringify(value)} onChange={e=>{try{onChange(JSON.parse(e.target.value))}catch{onChange(e.target.value)}}}/>;return <input className="table-input" value={String(value??'')} onChange={e=>onChange(e.target.value)}/>}
