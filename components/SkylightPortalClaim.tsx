'use client'
import{useState}from'react'
export function SkylightPortalClaim({token}:{token:string}){const[msg,setMsg]=useState(''),[busy,setBusy]=useState(false);const claim=async()=>{setBusy(true);setMsg('');try{const r=await fetch('/api/account/skylight',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({action:'claim',token})}),b=await r.json().catch(()=>({}));if(!r.ok)throw new Error(String(b.error||'Unable to connect workspace.'));setMsg('Workspace connected. Opening your Skylight customer portal…');window.location.href='/account/skylight?connected=1'}catch(e:any){setMsg(e.message)}finally{setBusy(false)}};return <div className="card" style={{maxWidth:720,margin:'0 auto',padding:28}}><div className="kpi">Secure Skylight Access</div><h1>Connect your Skylight customer workspace</h1><p className="muted">This invitation is tied to the email address that received it. Sign in or create your Local Pros account using that same email, then connect proposals, agreements, projects and invoices to your private workspace.</p>{msg?<div className="notice">{msg}</div>:null}<button className="btn btn-primary" disabled={busy} onClick={()=>void claim()}>{busy?'Connecting…':'Connect My Skylight Workspace'}</button><p className="small muted" style={{marginTop:16}}>Connecting a Skylight customer workspace does not affect Local Pros verification, organic ranking or Sponsored placement.</p></div>}
