import{SiteShell}from'./SiteShell';import{SearchForm}from'./SearchForm';import{BusinessCard}from'./BusinessCard';import{getBusinesses,getCategories}from'@/lib/data'
export async function VerticalPage({vertical,title,description}:{vertical:string;title:string;description:string}){const[cats,businesses]=await Promise.all([getCategories(vertical),getBusinesses({vertical,limit:60})]);return <SiteShell><main><section className="pagehero"><div className="container"><div className="crumb">Home › {title}</div><h1>{title}</h1><p>{description}</p></div></section><section className="section"><div className="container"><SearchForm categories={cats}/><div className="grid grid-4" style={{marginTop:24}}>{cats.map(c=><a className="card" key={c.id} href={`/search?category=${c.slug}`}><strong>{c.name}</strong><p className="muted small">Browse local {c.name.toLowerCase()}</p></a>)}</div><div className="section-head" style={{marginTop:45}}><div><h2>Published {title}</h2><p className="muted">Listings are displayed from the live directory inventory.</p></div></div><div className="business-list">{businesses.map(b=><BusinessCard key={b.id} business={b}/>)}</div>{!businesses.length&&<div className="empty">No published listings match this section yet.</div>}</div></section></main></SiteShell>}
