import Link from'next/link';import{SiteShell}from'./SiteShell';import{SearchForm}from'./SearchForm';import{BusinessCard}from'./BusinessCard';import{FeaturedBusinessSidebar}from'./FeaturedBusinessSidebar';import{getBusinesses,getCategories,getLocations,getGuides,getFeaturedSidebarBusinesses}from'@/lib/data';import{getSiteUrl}from'@/lib/site-url'
const guideTypeByVertical:Record<string,string>={home:'Homeowner Guide',legal:'Legal Guide',restaurant:'Dining Guide',retail:'Shopping Guide'};const pathByVertical:Record<string,string>={home:'/home-services',legal:'/legal-services',restaurant:'/restaurants',retail:'/local-stores'};const placementByVertical:Record<string,string>={home:'home_services_sidebar',legal:'attorney_sidebar',restaurant:'restaurant_sidebar',retail:'local_stores_sidebar'};
export async function VerticalPage({vertical,title,description}:{vertical:string;title:string;description:string}){const path=pathByVertical[vertical]||'/',[cats,businesses,locations,guides,featured]=await Promise.all([getCategories(vertical),getBusinesses({vertical,limit:60}),getLocations(),getGuides(120),getFeaturedSidebarBusinesses({pagePath:path,placement:placementByVertical[vertical],limit:4})]);const categoryNames=new Set(cats.map(c=>c.name));const preferredType=guideTypeByVertical[vertical];const relevant=(guides as any[]).filter(g=>(preferredType&&g.type===preferredType)||(g.category&&categoryNames.has(g.category))).slice(0,3),base=getSiteUrl(),canonical=`${base}${path}`,schema=[{'@context':'https://schema.org','@type':'BreadcrumbList',itemListElement:[{'@type':'ListItem',position:1,name:'Home',item:base},{'@type':'ListItem',position:2,name:title,item:canonical}]},...businesses.length?[{'@context':'https://schema.org','@type':'ItemList',name:`Published ${title} in Central Illinois`,itemListOrder:'https://schema.org/ItemListUnordered',numberOfItems:businesses.length,itemListElement:businesses.map((b:any,i:number)=>({'@type':'ListItem',position:i+1,item:{'@type':'LocalBusiness',name:b.name,url:`${base}/business/${b.slug}`}}))}]:[]];return <SiteShell><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(schema).replace(/</g,'\\u003c')}}/><main><section className="pagehero"><div className="container"><div className="crumb">Home › {title}</div><h1>{title}</h1><p>{description}</p></div></section><section className="section"><div className="container"><SearchForm categories={cats} locations={locations}/><div className="featured-content-layout" style={{marginTop:24}}><div className="featured-content-main"><div className="grid grid-4">{cats.map(c=><Link className="card" key={c.id} href={`/search?category=${c.slug}`}><strong>{c.name}</strong><p className="muted small">Browse local {c.name.toLowerCase()}</p></Link>)}</div>{relevant.length>0&&<section className="inline-guide-section"><div className="section-head compact-head"><div><div className="kpi">Helpful local reading</div><h2>{title} Guides</h2><p className="muted">Practical articles to help you plan, compare and make better-informed local decisions.</p></div><Link href="/guides">All Local Guides →</Link></div><div className="inline-guide-grid">{relevant.map((g:any)=><Link className="inline-guide-card" key={g.id} href={`/guides/${g.slug}`}><span>{g.city?`${g.city}, IL`:g.type||'Local Guide'}</span><strong>{g.title}</strong><p>{g.summary}</p></Link>)}</div></section>}<div className="section-head" style={{marginTop:45}}><div><h2>Published {title}</h2><p className="muted">Listings are displayed from the live directory inventory. Paid Featured inventory is shown separately.</p></div></div><div className="business-list">{businesses.map(b=><BusinessCard key={b.id} business={b}/>)}</div>{!businesses.length&&<div className="empty">No published listings match this section yet.</div>}</div><FeaturedBusinessSidebar businesses={featured as any[]} contextLabel={title.toLowerCase()}/></div></div></section></main></SiteShell>}
