import{bulkImportSubmissions}from'@/app/admin/bulk-actions';export function BulkImport(){return <><h1>Bulk Import</h1><p className="muted">Paste CSV with the exact headers below. Imports enter the approval queue instead of publishing directly.</p><div className="notice">business_name,category,city,phone,website,description,contact_name,email,service_areas</div><form action={bulkImportSubmissions} className="admin-card" style={{marginTop:14}}><label>CSV Data<textarea name="csv" placeholder="business_name,category,city,phone,..." required style={{minHeight:280}}/></label><button className="btn btn-primary">Import to Approval Queue</button></form></>}
