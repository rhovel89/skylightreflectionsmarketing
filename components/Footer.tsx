import Link from 'next/link';import {BrandLogo} from './BrandLogo';import type{NavItem,SiteSettings}from'@/lib/types'
export function Footer({site,navigation=[]}:{site:SiteSettings;navigation?:NavItem[]}){const groups=['footer_find','footer_explore','footer_business','footer_company','footer_legal'];const labels:Record<string,string>={footer_find:'Find a Pro',footer_explore:'Explore',footer_business:'For Businesses',footer_company:'Company',footer_legal:'Legal'};return <footer className="footer"><div className="container footer-grid"><div><BrandLogo site={site}/><p>{site.consumer_tagline}</p></div>{groups.map(g=><div key={g}><h4>{labels[g]}</h4>{navigation.filter(n=>n.menu_key===g&&n.is_visible).sort((a,b)=>a.sort_order-b.sort_order).map(n=><Link key={n.id} href={n.href}>{n.label}</Link>)}</div>)}</div><div className="staff-login-row"><Link href="/login?next=/admin" className="staff-login">Staff / Admin Login</Link></div></footer>}
