'use client'
import { useState } from 'react'
export function OutreachUnsubscribe({token}:{token:string}){const[state,setState]=useState<'idle'|'busy'|'done'|'error'>('idle');async function confirm(){setState('busy');const r=await fetch('/api/skylight-outreach/unsubscribe',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({token})}).catch(()=>null);setState(r?.ok?'done':'error')}if(state==='done')return <div className="notice"><strong>Preference saved.</strong> This outreach channel is now suppressed.</div>;return <div><button type="button" className="btn btn-primary" disabled={!token||state==='busy'} onClick={confirm}>{state==='busy'?'Saving…':'Confirm — Stop Outreach'}</button>{state==='error'?<div className="notice warn" style={{marginTop:12}}>We could not update the preference. Please try again.</div>:null}</div>}
