import{SiteShell}from'@/components/SiteShell';import{BusinessCard}from'@/components/BusinessCard';import{SearchForm}from'@/components/SearchForm';import{getBusinesses,getCategories}from'@/lib/data';export const dynamic='force-dynamic';export default async function Page({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const sp=await searchParams;const cats=await getCategories();const businesses=await getBusinesses({q:sp.q,city:sp.city,category:sp.category,vertical:sp.vertical,limit:60});return <SiteShell><main><section className="pagehero"><div className="container"><h1>Search Local Businesses</h1><SearchForm categories={cats} initial={sp}/></div></section><section className="section"><div className="container"><p className="muted">{businesses.length} matching published businesses</p><div className="business-list">{businesses.map(b=><BusinessCard key={b.id} business={b}/>)}</div></div></section></main></SiteShell>}
