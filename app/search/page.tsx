import Link from'next/link';import{SiteShell}from'@/components/SiteShell';import{BusinessCard}from'@/components/BusinessCard';import{SearchForm}from'@/components/SearchForm';import{getBusinesses,getCategories,getLocations,recordListingEvents,recordSearchEvent}from'@/lib/data';export const dynamic='force-dynamic';export default async function Page({searchParams}:{searchParams:Promise<Record<string,string|string[]|undefined>>}){const sp=await searchParams;const category=typeof sp.category==='string'?sp.category:undefined,city=typeof sp.city==='string'?sp.city:undefined,q=typeof sp.q==='string'?sp.q:undefined;const[cats,locations,businesses]=await Promise.all([getCategories(),getLocations(),getBusinesses({category,city,q,limit:150})]);const catLabel=cats.find(c=>c.slug===category)?.name;const cityLabel=locations.find(l=>l.slug===city)?.name;const filters=[catLabel,cityLabel,q?.trim()?`“${q.trim()}”`:undefined].filter(Boolean) as string[];await Promise.all([recordSearchEvent(catLabel||q?.trim()||undefined,cityLabel,businesses.length),recordListingEvents(businesses.map(b=>b.id),'impression')]);return <SiteShell><main><section className="pagehero"><div className="container"><div className="eyebrow">Central Illinois Local Search</div><h1>Find Local Businesses</h1><p>Search published local profiles by category, city, or business name. Sponsored placement is labeled and does not replace organic relevance.</p></div></section><section className="section"><div className="container"><div className="search-panel"><SearchForm categories={cats} defaults={{category,city,q}}/></div><div className="results-toolbar"><div><span className="kpi">{businesses.length} result{businesses.length===1?'':'s'}</span>{filters.length>0&&<div className="filter-pills">{filters.map(f=><span className="filter-pill" key={f}>{f}</span>)}</div>}</div>{filters.length>0&&<Link className="btn btn-light" href="/search">Clear filters</Link>}</div>{businesses.length?<div className="business-list">{businesses.map(b=><BusinessCard key={b.id} business={b}/>)}</div>:<div className="empty empty-rich"><h2>No matching businesses found</h2><p>Try removing one filter, choosing a nearby Central Illinois market, or searching by the business name.</p><Link className="btn btn-primary" href="/search">Browse all published businesses</Link></div>}<div className="section-head search-browse-head"><div><h2>Browse Popular Categories</h2><p className="muted">Prefer to browse? Start with a category and narrow by city.</p></div></div><div className="grid grid-4">{cats.slice(0,12).map(c=><Link className="card category-card" key={c.id} href={`/search?category=${encodeURIComponent(c.slug)}`}><strong>{c.name}</strong><p className="small muted">View published local profiles</p></Link>)}</div></div></section></main></SiteShell>}
