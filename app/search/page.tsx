import{SiteShell}from'@/components/SiteShell';import{BusinessCard}from'@/components/BusinessCard';import{SearchForm}from'@/components/SearchForm';import{getBusinesses,getCategories}from'@/lib/data';export const dynamic='force-dynamic';export default async function Page({searchParams}:{searchParams:Promise<Record<string,string|string[]|undefined>>}){const sp=await searchParams;const category=typeof sp.category==='string'?sp.category:undefined,city=typeof sp.city==='string'?sp.city:undefined,q=typeof sp.q==='string'?sp.q:undefined;const[cats,businesses]=await Promise.all([getCategories(),getBusinesses({category,city,q,limit:150})]);return <SiteShell><main><section className="pagehero"><div className="container"><h1>Search Central Illinois Local Pros</h1><p>Filter by category, city or business name.</p></div></section><section className="section"><div className="container"><SearchForm categories={cats}/><p className="muted" style={{marginTop:20}}>{businesses.length} published result{businesses.length===1?'':'s'}</p><div className="business-list">{businesses.map(b=><BusinessCard key={b.id} business={b}/>)}</div></div></section></main></SiteShell>}