import { OutreachUnsubscribe } from '@/components/OutreachUnsubscribe'
export const metadata={title:'Outreach Preference | Skylight Reflections Marketing',robots:{index:false,follow:false,noarchive:true}}
export default async function Page({searchParams}:{searchParams:Promise<Record<string,string|string[]|undefined>>}){const sp=await searchParams;const token=Array.isArray(sp.token)?sp.token[0]||'':sp.token||'';return <main className="container section"><div className="card" style={{maxWidth:640,margin:'40px auto'}}><div className="kpi">Skylight Reflections Marketing</div><h1>Stop outreach to this email</h1><p className="muted">Confirm below and this outreach channel will be suppressed. This does not change any Central Illinois Local Pros listing, verification, organic ranking, Sponsored placement, Lead Buyer agreement, or billing state.</p><OutreachUnsubscribe token={token}/></div></main>}
