import{NextResponse}from'next/server';import{createClient}from'@/lib/supabase/server'
export async function POST(req:Request){try{const body=await req.json() as{recipient_id?:string;status?:string;owner_notes?:string};if(!body.recipient_id||!body.status)return NextResponse.json({error:'Lead recipient and status are required.'},{status:400});const s=await createClient();const{error}=await s.rpc('update_owner_lead_recipient',{p_recipient_id:body.recipient_id,p_status:body.status,p_owner_notes:body.owner_notes||null});if(error)return NextResponse.json({error:error.message},{status:error.message.includes('insufficient_privilege')||error.message.includes('authentication_required')?403:400});return NextResponse.json({ok:true})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Unable to update lead.'},{status:400})}}
