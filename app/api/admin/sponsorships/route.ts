import{NextResponse}from'next/server';import{createClient}from'@/lib/supabase/server'
export async function POST(req:Request){try{const b=await req.json() as{business_id?:string;market_location_id?:string|null;category_id?:string|null;placement?:string;starts_on?:string|null;ends_on?:string|null;active?:boolean};if(!b.business_id||!b.placement)return NextResponse.json({error:'Business and placement are required.'},{status:400});const s=await createClient();const{data,error}=await s.from('sponsorships').insert({business_id:b.business_id,market_location_id:b.market_location_id||null,category_id:b.category_id||null,placement:b.placement,starts_on:b.starts_on||null,ends_on:b.ends_on||null,active:b.active!==false}).select('id').single();if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json({ok:true,id:data.id})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Unable to create sponsorship.'},{status:400})}}
