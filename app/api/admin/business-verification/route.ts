import{NextResponse}from'next/server';import{createClient}from'@/lib/supabase/server'
type Body={id:string;decision:'verify'|'unverify';source_name?:string;source_url?:string}
export async function POST(req:Request){try{const body=await req.json() as Body;if(!body?.id||!['verify','unverify'].includes(body.decision))return NextResponse.json({error:'Invalid business verification request.'},{status:400});const s=await createClient();const{data,error}=await s.rpc('review_business_verification',{p_business_id:body.id,p_decision:body.decision,p_source_name:body.source_name||null,p_source_url:body.source_url||null});if(error)return NextResponse.json({error:error.message},{status:error.message.includes('insufficient_privilege')||error.message.includes('authentication_required')?403:400});return NextResponse.json({ok:true,result:data})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Unable to update business verification.'},{status:400})}}
