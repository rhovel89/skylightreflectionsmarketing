import{NextResponse}from'next/server';import{createClient}from'@/lib/supabase/server';
export async function POST(req:Request){try{const body=await req.json() as any,businessId=String(body.business_id||'');if(!businessId)return NextResponse.json({error:'Business is required.'},{status:400});const s=await createClient();const{data,error}=await s.from('business_lead_programs').update({email_delivery_notifications:Boolean(body.email_delivery_notifications),sms_delivery_notifications:Boolean(body.sms_delivery_notifications),notification_email:String(body.notification_email||'').trim()||null,notification_phone:String(body.notification_phone||'').trim()||null}).eq('business_id',businessId).select('id').maybeSingle();if(error)return NextResponse.json({error:error.message},{status:400});if(!data)return NextResponse.json({error:'Super Admin access and an existing lead agreement are required.'},{status:403});return NextResponse.json({ok:true,message:'Lead delivery notification preferences saved.'})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Unable to save notification preferences.'},{status:400})}}
