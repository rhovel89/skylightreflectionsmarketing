import{NextResponse}from'next/server';import{createClient}from'@/lib/supabase/server';
export async function POST(req:Request){try{const body=await req.json() as any,businessId=String(body.business_id||'');if(!businessId)return NextResponse.json({error:'Business is required.'},{status:400});const s=await createClient();const{data:business}=await s.from('businesses').select('tenant_id').eq('id',businessId).maybeSingle();if(!business)return NextResponse.json({error:'Business not found.'},{status:404});const{data:roles}=await s.from('user_roles').select('role').eq('tenant_id',business.tenant_id).eq('role','super_admin').limit(1);if(!roles?.length)return NextResponse.json({error:'Super Admin access is required.'},{status:403});const{error}=await s.from('business_lead_programs').update({email_delivery_notifications:Boolean(body.email_delivery_notifications),sms_delivery_notifications:Boolean(body.sms_delivery_notifications),notification_email:String(body.notification_email||'').trim()||null,notification_phone:String(body.notification_phone||'').trim()||null}).eq('business_id',businessId);if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json({ok:true,message:'Lead delivery notification preferences saved.'})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Unable to save notification preferences.'},{status:400})}}
