import{SiteShell}from'@/components/SiteShell';import{TENANT_ID}from'@/lib/constants';import{createClient}from'@/lib/supabase/server';import{redirect}from'next/navigation';export const dynamic='force-dynamic';async function submitBusiness(fd:FormData){'use server';const s=await createClient();await s.from('business_submissions').insert({tenant_id:TENANT_ID,business_name:String(fd.get('business_name')||''),category:String(fd.get('category')||''),city:String(fd.get('city')||''),phone:String(fd.get('phone')||''),website:String(fd.get('website')||'')||null,description:String(fd.get('description')||'')||null,status:'pending',contact_name:String(fd.get('contact_name')||'')||null,email:String(fd.get('email')||'')||null,service_areas:String(fd.get('service_areas')||'').split(',').map(x=>x.trim()).filter(Boolean),consent_to_contact:fd.get('consent')==='on',source:'public_site'});redirect('/contact?submitted=business')}async function marketing(fd:FormData){'use server';const s=await createClient();await s.from('marketing_leads').insert({tenant_id:TENANT_ID,business_name:String(fd.get('business_name')||''),contact_name:String(fd.get('contact_name')||''),phone:String(fd.get('phone')||'')||null,email:String(fd.get('email')||''),service_interest:String(fd.get('service_interest')||'')||null,message:String(fd.get('message')||'')||null,status:'new',consent_to_contact:fd.get('consent')==='on',source:'directory_contact'});redirect('/contact?submitted=marketing')}export default function Page(){return <SiteShell><main><section className="pagehero"><div className="container"><h1>Contact & Business Submissions</h1><p>Connect with the directory or request a Skylight Reflections Marketing visibility review.</p></div></section><section className="section"><div className="container grid grid-2"><form action={submitBusiness} className="form-card"><h2>Submit a Business</h2><label>Business Name<input name="business_name" required/></label><div className="form-grid"><label>Category<input name="category" required/></label><label>City<input name="city" required/></label><label>Phone<input name="phone" required/></label><label>Website<input name="website"/></label><label>Contact Name<input name="contact_name"/></label><label>Email<input type="email" name="email"/></label></div><label>Service Areas<input name="service_areas" placeholder="Pontiac, Dwight, Livingston County"/></label><label>Description<textarea name="description"/></label><label className="check"><input type="checkbox" name="consent" required/> I agree to be contacted about this submission.</label><button className="btn btn-primary">Submit for Review</button></form><form action={marketing} className="form-card"><h2>Free Marketing Visibility Review</h2><p className="muted small">This request goes to Skylight Reflections Marketing and is separate from organic directory ranking.</p><label>Business Name<input name="business_name" required/></label><label>Your Name<input name="contact_name" required/></label><div className="form-grid"><label>Phone<input name="phone"/></label><label>Email<input type="email" name="email" required/></label></div><label>What would you like help with?<select name="service_interest"><option>Website Design / Redesign</option><option>Local SEO</option><option>Google Business Profile</option><option>Social Media Management</option><option>Branding / Graphic Design</option><option>Lead Generation</option><option>Full Digital Marketing Review</option></select></label><label>What are you trying to improve?<textarea name="message"/></label><label className="check"><input type="checkbox" name="consent" required/> I agree to be contacted about this request.</label><button className="btn btn-primary">Request My Free Review</button></form></div></section></main></SiteShell>}