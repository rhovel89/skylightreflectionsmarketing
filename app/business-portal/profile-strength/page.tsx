import Link from'next/link';import{getOwnerData}from'@/lib/owner';
function rel(v:any){return Array.isArray(v)?v[0]:v}
function activeSub(x:any){return['active','trialing','past_due'].includes(String(x?.status||''))&&(!x?.ends_at||new Date(x.ends_at).getTime()>Date.now())}
export default async function Page({searchParams}:{searchParams:Promise<Record<string,string|string[]|undefined>>}){const sp=await searchParams;const{businesses,s}=await getOwnerData('/business-portal/profile-strength');if(!businesses.length)return <div className="card empty-rich"><h2>Claim a business first</h2><p className="muted">Profile Strength becomes available after staff approves a legitimate ownership claim.</p><Link className="btn btn-primary" href="/search">Find My Listing</Link></div>;const requested=typeof sp.business==='string'?sp.business:'';const b=businesses.find((x:any)=>x.id===requested)??businesses[0];const switcher=businesses.length>1?<form className="portal-switcher" action="/business-portal/profile-strength" method="get"><label>Managing<select name="business" defaultValue={b.id}>{businesses.map((x:any)=><option key={x.id} value={x.id}>{x.name}</option>)}</select></label><button className="btn btn-light" type="submit">Switch Business</button></form>:<div className="portal-current"><span>Managing</span><strong>{b.name}</strong></div>;const[{data:media},{data:branches},{data:cats},{data:areas},{data:subs}]=await Promise.all([s.from('business_media').select('media_type,status,approval_status').eq('business_id',b.id),s.from('business_locations').select('id,is_active,is_primary,address_text,city,state').eq('business_id',b.id).eq('is_active',true),s.from('business_categories').select('category_id,categories(name,vertical)').eq('business_id',b.id),s.from('business_service_areas').select('location_id,locations(name,slug)').eq('business_id',b.id),s.from('subscriptions').select('status,ends_at,updated_at,plans(slug)').eq('business_id',b.id).in('status',['active','trialing','past_due']).order('updated_at',{ascending:false}).limit(10)]);const approvedMedia=(media??[]).filter((x:any)=>x.approval_status==='approved'||x.status==='published'||x.status==='active'),hasLogo=approvedMedia.some((x:any)=>x.media_type==='logo'),hasVisual=approvedMedia.some((x:any)=>['cover','gallery'].includes(x.media_type)),galleryCount=approvedMedia.filter((x:any)=>x.media_type==='gallery').length,desc=String(b.description||'').trim(),checks=[{label:'Business description',points:15,done:Boolean(desc),action:'Add a clear customer-facing description.',href:`/business-portal/listing?business=${b.id}`},{label:'Helpful description depth',points:5,done:desc.length>=120,action:'Expand the description to explain services, customers served and what makes the business useful.',href:`/business-portal/listing?business=${b.id}`},{label:'Phone number',points:15,done:Boolean(b.phone),action:'Add a current customer phone number.',href:`/business-portal/listing?business=${b.id}`},{label:'Website',points:10,done:Boolean(b.website),action:'Add the current business website if one is available.',href:`/business-portal/listing?business=${b.id}`},{label:'Business hours',points:10,done:Boolean(b.hours),action:'Add normal business hours or availability information.',href:`/business-portal/listing?business=${b.id}`},{label:'Directory category',points:10,done:Boolean(cats?.length),action:'Request the correct business category so customers can understand what you provide.',href:'/contact'},{label:'Local presence',points:15,done:Boolean(branches?.length||areas?.length),action:'Confirm a real physical location or accurately labeled service areas.',href:`/business-portal/areas?business=${b.id}`},{label:'Business logo',points:5,done:hasLogo,action:'Upload an approved logo so customers recognize the business.',href:`/business-portal/media?business=${b.id}`},{label:'Profile visuals',points:5,done:hasVisual,action:'Add a cover or approved gallery image.',href:`/business-portal/media?business=${b.id}`},{label:'Claimed ownership',points:5,done:Boolean(b.claimed),action:'Complete the legitimate ownership claim workflow.',href:'/claim'}],score=checks.reduce((n,x)=>n+(x.done?x.points:0),0),status=score>=90?'Excellent':score>=75?'Strong':score>=50?'Building':'Needs attention',missing=checks.filter(x=>!x.done),pro=Boolean((subs??[]).find((x:any)=>activeSub(x)&&rel(x.plans)?.slug==='pro')),rawPro=b.attributes&&typeof b.attributes==='object'?(b.attributes as any).pro_profile||{}:{},today=new Date().toISOString().slice(0,10),conversion=[{label:'3+ services',done:Array.isArray(rawPro.services)&&rawPro.services.filter((x:any)=>x?.name).length>=3},{label:'3+ FAQs',done:Array.isArray(rawPro.faqs)&&rawPro.faqs.filter((x:any)=>x?.question&&x?.answer).length>=3},{label:'Primary CTA',done:Boolean(rawPro.cta?.primary_label&&rawPro.cta?.primary_url)},{label:'Current offer',done:Boolean(rawPro.offer?.title&&(!rawPro.offer?.expires_on||String(rawPro.offer.expires_on)>=today))},{label:'Package / product',done:Array.isArray(rawPro.packages)&&rawPro.packages.some((x:any)=>x?.name)},{label:'Social profile',done:Boolean(rawPro.social_links&&Object.values(rawPro.social_links).some(Boolean))},{label:'Current announcement',done:Array.isArray(rawPro.announcements)&&rawPro.announcements.some((x:any)=>x?.title&&x?.body)},{label:'3+ showcase images',done:galleryCount>=3}],conversionScore=Math.round(conversion.filter(x=>x.done).length/conversion.length*100);return <div>{switcher}<div className="portal-section-head"><div><div className="kpi">Customer-Safe Profile Health</div><h2>Profile Strength — {b.name}</h2><p className="muted">A practical completeness score based on customer-facing listing information. It does not expose internal SEO diagnostics and does not change organic ranking.</p></div><Link className="btn btn-light" href={`/business/${b.slug}`}>View Public Profile</Link></div><div className="grid grid-3"><div className="card"><div className="kpi">Profile Strength</div><h2>{score}% · {status}</h2><div className="progress-track"><span style={{width:`${score}%`}}/></div><p className="small muted">Only customer-facing completeness is scored here. Verified status and paid products are not required for a perfect base score.</p></div><div className="card"><div className="kpi">Local Coverage</div><h2>{(branches??[]).length} physical · {(areas??[]).length} served</h2><p className="small muted">Physical locations and service areas remain separate. Service areas are never represented as offices.</p></div><div className="card"><div className="kpi">Trust Status</div><h2>{b.verified?'Verified':'Not Verified'} · {b.claimed?'Claimed':'Unclaimed'}</h2><p className="small muted">Verification is a protected trust workflow and is not purchased through a directory plan.</p></div></div><div className="card" style={{marginTop:18}}><div className="section-head compact-head"><div><div className="kpi">Strength Checklist</div><h2>{checks.filter(x=>x.done).length} of {checks.length} profile signals complete</h2></div><span className={`badge ${score>=75?'verified':'neutral'}`}>{status}</span></div>{checks.map(x=><div className="info-row" key={x.label}><span>{x.label} · {x.points} pts</span><strong>{x.done?'Complete':'Needs attention'}</strong></div>)}</div>{missing.length>0&&<div className="card" style={{marginTop:18}}><div className="kpi">Recommended Next Actions</div><h2>Improve what customers see</h2><div className="request-list">{missing.slice(0,6).map(x=><div className="admin-card" key={x.label}><h3>{x.label}</h3><p className="small muted">{x.action}</p><Link className="btn btn-light" href={x.href}>Improve This</Link></div>)}</div></div>}{pro?<div className="card" style={{marginTop:18}}><div className="section-head compact-head"><div><div className="kpi">Pro Conversion Readiness</div><h2>{conversionScore}% ready</h2><p className="small muted">Optional conversion tools are measured separately from base Profile Strength and organic ranking.</p></div><strong className="profile-score">{conversionScore}%</strong></div><div className="progress-track"><span style={{width:`${conversionScore}%`}}/></div>{conversion.map(x=><div className="info-row" key={x.label}><span>{x.label}</span><strong>{x.done?'Ready':'Add it'}</strong></div>)}<div className="card-actions"><Link className="btn btn-primary" href={`/business-portal/pro-profile?business=${b.id}`}>Improve Pro Conversion Tools</Link><Link className="btn btn-light" href={`/business-portal/performance?business=${b.id}`}>View Performance</Link></div></div>:<div className="notice" style={{marginTop:18}}><strong>Optional Pro tools are excluded from your base score.</strong> Upgrading can add conversion features, but buying a plan is never required to improve the customer-safe Profile Strength score above.</div>}</div>}
