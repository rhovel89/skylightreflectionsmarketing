import Link from'next/link';import{SiteShell}from'@/components/SiteShell';import{SearchForm}from'@/components/SearchForm';import{BusinessCard}from'@/components/BusinessCard';import{getBusinesses,getCategories,getPublicConfig,getLocations,getGuides}from'@/lib/data';import{VERTICALS}from'@/lib/constants'
export const dynamic='force-dynamic'
export default async function Home(){const[cfg,categories,businesses,locations,guides]=await Promise.all([getPublicConfig(),getCategories(),getBusinesses({limit:8}),getLocations(),getGuides(3)]);const site=cfg.site??{};return <SiteShell><main><section className="hero"><div className="container hero-inner"><div className="eyebrow">{site.hero_eyebrow}</div><h1>{site.hero_title?.replace('Local Pro.','')}<span className="gradient-text">Local Pro.</span></h1><p>{site.hero_subtitle}</p><SearchForm categories={categories} locations={locations}/><div className="hero-proof"><span className="proof-pill">Local businesses</span><span className="proof-pill">Central Illinois markets</span><span className="proof-pill">Direct connections</span><span className="proof-pill">No pay-to-rank organic results</span></div></div></section><section className="section"><div className="container"><div className="section-head"><div><h2>Explore Central Illinois</h2><p className="muted">Four first-class local discovery verticals, built for useful profiles and genuine local connections.</p></div></div><div className="grid grid-4">{VERTICALS.map((v,i)=><div className="card category-card" key={v.key}><div className="category-icon">{['🏠','⚖️','🍽️','🛍️'][i]}</div><h3>{v.label}</h3><p className="muted">Browse local {v.label.toLowerCase()} by city and category.</p><Link className="btn btn-light" href={v.href}>Explore {v.label}</Link></div>)}</div></div></section><section className="section white"><div className="container"><div className="section-head"><div><h2>Local Businesses</h2><p className="muted">Published local profiles from the live directory database.</p></div><Link href="/search">Search all →</Link></div><div className="business-list">{businesses.map(b=><BusinessCard key={b.id} business={b}/>)}</div></div></section><section className="section"><div className="container"><div className="section-head"><div><h2>Browse by Location</h2><p className="muted">Start with an active Central Illinois market and expand into local categories.</p></div></div><div className="grid grid-4">{locations.slice(0,12).map(l=><Link className="card category-card" key={l.id} href={`/illinois/${l.slug}`}><strong>{l.name}, IL</strong><p className="muted small">View local businesses and guides</p></Link>)}</div></div></section>{guides.length>0&&<section className="section white"><div className="container"><div className="section-head"><div><div className="kpi">Local knowledge</div><h2>Latest Local Guides</h2><p className="muted">Practical Central Illinois articles for homeowners, diners, shoppers and people comparing local services.</p></div><Link href="/guides">Browse all guides →</Link></div><div className="inline-guide-grid">{guides.map((g:any)=><Link className="inline-guide-card" key={g.id} href={`/guides/${g.slug}`}><span>{g.city?`${g.city}, IL`:g.type||'Local Guide'}</span><strong>{g.title}</strong><p>{g.summary}</p></Link>)}</div></div></section>}<section className="section"><div className="container grid grid-2"><div className="card"><h2>Own a local business?</h2><p className="muted">Claim an existing profile or submit your business. Plans, pricing and profile tools are managed from the directory system.</p><Link className="btn btn-primary" href="/for-businesses">For Businesses</Link></div><div className="card"><h2>Powered by Skylight Reflections Marketing</h2><p className="muted">Local visibility, website design, SEO, Google Business Profile, social media, branding and lead-generation opportunities are managed separately from organic directory relevance.</p><Link className="btn btn-light" href="/contact">Contact Skylight</Link></div></div></section></main></SiteShell>}
