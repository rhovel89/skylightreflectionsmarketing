import{SiteShell}from'@/components/SiteShell';import{getGuides}from'@/lib/data';import Link from'next/link';export const dynamic='force-dynamic';export default async function Page(){const guides=await getGuides();return <SiteShell><main><section className="pagehero"><div className="container"><h1>Central Illinois Local Guides</h1><p>Useful local content designed to help people compare choices and understand their area.</p></div></section><section className="section"><div className="container grid grid-3">{guides.map((g:any)=><Link className="card" href={`/guides/${g.slug}`} key={g.id}><span className="badge neutral">{g.type}</span><h3>{g.title}</h3><p className="muted">{g.summary}</p></Link>)}{!guides.length&&<div className="empty">Published local guides will appear here.</div>}</div></section></main></SiteShell>}
