import{notFound}from'next/navigation';import{SiteShell}from'@/components/SiteShell';import{getGuide}from'@/lib/data';export const dynamic='force-dynamic';export default async function Page({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const g=await getGuide(slug);if(!g)notFound();return <SiteShell><main><section className="pagehero"><div className="container"><div className="crumb">Local Guides › {g.city||'Central Illinois'}</div><h1>{g.title}</h1><p>{g.summary}</p></div></section><section className="section"><article className="container card"><div style={{whiteSpace:'pre-wrap',lineHeight:1.75}}>{g.body}</div></article></section></main></SiteShell>}
