import{createClient}from'@/lib/supabase/server'
import{TENANT_ID}from'@/lib/constants'
import{requireAdmin}from'@/lib/auth'
import{SkylightServiceCatalogManager}from'@/components/SkylightServiceCatalogManager'
import{SkylightServiceOperationsDefaults}from'@/components/SkylightServiceOperationsDefaults'
export const dynamic='force-dynamic'
export default async function Page(){await requireAdmin('/admin/skylight-services');const s=await createClient();const[{data:services},{data:mappings}]=await Promise.all([s.from('skylight_service_catalog').select('*').eq('tenant_id',TENANT_ID).order('sort_order'),s.from('skylight_signal_service_map').select('*').eq('tenant_id',TENANT_ID).order('signal_key')]);const svc=(services??[]) as any[];return <><div className="admin-page-head"><div><div className="kpi">Skylight Reflections Marketing</div><h1>Services & Pricing</h1><p className="muted">Add any service you want, then customize its pricing, billing cadence, deposit, delivery timeframe, proposal scope, agreement requirement, project workflow, onboarding questions and required client files without a code change.</p><div style={{display:'flex',gap:10,flexWrap:'wrap',marginTop:12}}><a className="btn btn-primary" href="/admin/skylight-operations">Proposals & Projects</a><a className="btn btn-primary" href="/admin/skylight-intake">Intake & Client Assets</a><a className="btn btn-light" href="/admin/skylight-eddm">Manage EDDM & Community Mailers</a><a className="btn btn-light" href="/skylight-services">View Public Services</a></div></div><span className="badge verified">Admin Only</span></div><SkylightServiceCatalogManager services={svc} mappings={(mappings??[]) as any[]}/><div style={{height:18}}/><SkylightServiceOperationsDefaults services={svc}/></>}
