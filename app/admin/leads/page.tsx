import{createClient}from'@/lib/supabase/server';import{TENANT_ID}from'@/lib/constants';import{AdminLeadOperations}from'@/components/AdminLeadOperations'
export default async function Page(){const s=await createClient();const[{data:leads,error},{data:businesses}]=await Promise.all([s.from('leads').select('id,service,city,consumer_name,phone,email,message,timeline,status,source,created_at,lead_recipients(id,business_id,status,route_reason,route_rank,routed_at,businesses(name,slug))').eq('tenant_id',TENANT_ID).order('created_at',{ascending:false}).limit(100),s.from('businesses').select('id,name').eq('tenant_id',TENANT_ID).eq('status','published').order('name').limit(500)]);return <><div className="admin-page-head"><div><div className="kpi">Lead Operations</div><h1>Consumer Leads</h1><p className="muted">Route new requests transparently, monitor recipient outcomes, and keep paid sponsorship separate from organic relevance and lead route rank.</p></div><span className="badge sponsored">Workflow controlled</span></div>{error?<div className="notice warn">{error.message}</div>:<AdminLeadOperations rows={(leads??[]) as any[]} businesses={(businesses??[]) as any[]}/>}</>}
