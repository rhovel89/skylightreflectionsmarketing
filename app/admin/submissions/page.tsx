import { createClient } from '@/lib/supabase/server'
import { TENANT_ID } from '@/lib/constants'
import { AdminModerationQueue } from '@/components/AdminModerationQueue'

export const dynamic='force-dynamic'
export default async function Page(){const s=await createClient();const{data,error}=await s.from('business_submissions').select('id,business_name,category,city,phone,email,website,description,status,contact_name,service_areas,operating_model,address_text,state,postal_code,hours,price_range,menu_url,ordering_url,reservation_url,marketing_opt_in,completed_at,promoted_business_id,claim_invited_at,created_at,reviewed_by,reviewed_at,review_notes,source,business_submission_media(id,media_type,original_filename,status,sort_order)').eq('tenant_id',TENANT_ID).order('created_at',{ascending:false}).limit(150);return <><div className="admin-page-head"><div><div className="kpi">Protected Onboarding Review</div><h1>Business Profile Approval Queue</h1><p className="muted">Review completed business onboarding profiles and staged media. Approval creates a pending canonical profile and sends the ownership-claim invitation; it does not publish the listing.</p></div><span className="badge sponsored">Profile gate</span></div><div className="notice"><strong>Workflow:</strong> Profile approval → owner claim/evidence → ownership approval → source-backed verification → publish. Paid plans and Sponsored placement are never required for these trust gates.</div>{error?<div className="notice warn">{error.message}</div>:<AdminModerationQueue kind="submissions" rows={(data??[]) as any[]}/>}</>}
