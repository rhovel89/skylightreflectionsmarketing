import{notFound}from'next/navigation';import{createClient}from'@/lib/supabase/server';import{TENANT_ID}from'@/lib/constants';import{ADMIN_ENTITIES}from'@/lib/admin';import{AdminEntityEditor}from'@/components/AdminEntityEditor';import{SiteBuilder}from'@/components/SiteBuilder';import{PricingEditor}from'@/components/PricingEditor';import{NavigationEditor}from'@/components/NavigationEditor';import{TeamRoles}from'@/components/TeamRoles';import{AdminCreateForm}from'@/components/AdminCreateForm';import{BulkImport}from'@/components/BulkImport';export default async function Page({params}:{params:Promise<{section:string}>}){const{section}=await params;if(section==='site-builder')return <SiteBuilder/>;if(section==='pricing')return <PricingEditor/>;if(section==='navigation')return <NavigationEditor/>;if(section==='team')return <TeamRoles/>;if(section==='bulk-import')return <BulkImport/>;const cfg=ADMIN_ENTITIES[section];if(!cfg)notFound();const s=await createClient();let q=s.from(cfg.table).select(cfg.select).limit(100);if(!['business_claims','sponsorships','subscriptions','listing_daily_stats'].includes(cfg.table))q=q.eq('tenant_id',TENANT_ID);if(cfg.order)q=q.order(cfg.order,{ascending:false});const{data,error}=await q;return <><h1>{cfg.title}</h1><AdminCreateForm section={section}/>{error?<div className="notice warn">{error.message}</div>:<AdminEntityEditor section={section} cfg={cfg} rows={(data??[]) as unknown as Record<string,unknown>[]}/>}</>}