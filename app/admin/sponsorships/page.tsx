import{createClient}from'@/lib/supabase/server';import{TENANT_ID}from'@/lib/constants';import{requireSuperAdmin}from'@/lib/auth';import{SponsorshipManager}from'@/components/SponsorshipManager'
export const dynamic='force-dynamic';
export default async function Page(){await requireSuperAdmin('/admin/sponsorships');const s=await createClient();const[{data:rows,error},{data:businessRows},{data:locations},{data:categories},{data:logos}]=await Promise.all([s.from('sponsorships').select('id,business_id,market_location_id,category_id,placement,page_path,starts_on,ends_on,active,priority,sort_order,rotation_weight,origin,provider,provider_subscription_id,created_at,updated_at').eq('tenant_id',TENANT_ID).order('created_at',{ascending:false}).limit(250),s.from('businesses').select('id,name').eq('tenant_id',TENANT_ID).eq('status','published').order('name').limit(2000),s.from('locations').select('id,name').eq('tenant_id',TENANT_ID).eq('is_active',true).order('name'),s.from('categories').select('id,name').eq('tenant_id',TENANT_ID).eq('is_active',true).order('name'),s.from('business_media').select('business_id,storage_path,sort_order,created_at').eq('tenant_id',TENANT_ID).eq('media_type','logo').eq('status','active').eq('approval_status','approved').order('sort_order',{ascending:true}).order('created_at',{ascending:false}).limit(3000)]);const logoMap=new Map<string,string>();for(const row of logos??[]){if(logoMap.has(row.business_id))continue;logoMap.set(row.business_id,s.storage.from('business-media').getPublicUrl(row.storage_path).data.publicUrl)}const businesses=(businessRows??[]).map((b:any)=>({...b,logo_url:logoMap.get(b.id)||null}));return <><div className="admin-page-head"><div><div className="kpi">Super Admin · Featured / Sponsored Controls</div><h1>Featured & Sponsored Placement Manager</h1><p className="muted">Choose exactly which published businesses appear in Featured inventory, preview their approved logo, target placement, set order/rotation, schedule dates, pause or remove placements. Paid placement remains separate from organic relevance, verification, SEO coverage and lead-routing rank.</p></div><span className="badge sponsored">Super Admin controlled</span></div>{error?<div className="notice warn">{error.message}</div>:<SponsorshipManager rows={(rows??[]) as any[]} businesses={businesses as any[]} locations={(locations??[]) as any[]} categories={(categories??[]) as any[]}/>}</>}
