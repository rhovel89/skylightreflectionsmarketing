import{type EmailOtpType}from'@supabase/supabase-js';import{NextResponse,type NextRequest}from'next/server';import{createClient}from'@/lib/supabase/server';export async function GET(req:NextRequest){const{searchParams}=new URL(req.url),token_hash=searchParams.get('token_hash'),type=searchParams.get('type') as EmailOtpType|null,next=searchParams.get('next')||'/account';const target=req.nextUrl.clone();target.pathname=next.startsWith('/')?next:'/account';target.search='';if(token_hash&&type){const s=await createClient();const{error}=await s.auth.verifyOtp({type,token_hash});if(!error)return NextResponse.redirect(target)}target.pathname='/error';return NextResponse.redirect(target)}
