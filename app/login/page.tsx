import{SiteShell}from'@/components/SiteShell';import{login,signup}from'./actions';export default async function Page({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const sp=await searchParams;const next=sp.next||'/account';return <SiteShell><main className="auth-wrap"><div className="auth-card"><div className="eyebrow" style={{color:'#4a00e0'}}>Secure Account Access</div><h1>Sign In</h1><p className="muted">Consumers, business owners and staff use Supabase Auth. Staff/Admin access also requires an authorized role.</p>{sp.error&&<div className="form-status error">{sp.error}</div>}{sp.message&&<div className="form-status success">{sp.message}</div>}<form><input type="hidden" name="next" value={next}/><label>Email<input name="email" type="email" required/></label><label>Password<input name="password" type="password" minLength={8} required/></label><div className="card-actions"><button className="btn btn-primary" formAction={login}>Sign In</button><button className="btn btn-light" formAction={signup}>Create Account</button></div></form></div></main></SiteShell>}
