import type{Metadata}from'next';import{SiteShell}from'@/components/SiteShell';import{login,signup}from'./actions';export const metadata:Metadata={title:'Secure Sign In | Central Illinois Local Pros',robots:{index:false,follow:false}};export default async function Page({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const sp=await searchParams;const next=sp.next||'/account';return <SiteShell><main className="auth-wrap"><div style={{width:'min(980px,100%)'}}><div className="eyebrow" style={{color:'#4a00e0',textAlign:'center',marginBottom:10}}>Secure Account Access</div><h1 style={{textAlign:'center',marginBottom:8}}>Sign in or create your Local Pros account</h1><p className="muted" style={{textAlign:'center',maxWidth:720,margin:'0 auto 22px'}}>Anyone who wants to list or claim a business must have an account with a unique username and password. Your email remains the secure sign-in, recovery and notification credential.</p>{sp.error&&<div className="form-status error">{sp.error}</div>}{sp.message&&<div className="form-status success">{sp.message}</div>}<div style={{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(300px,1fr))',gap:18,alignItems:'start'}}><section className="auth-card" style={{width:'100%'}}><div className="kpi">Already have an account?</div><h2>Sign In</h2><p className="muted small">Use the email and password attached to your account.</p><form><input type="hidden" name="next" value={next}/><label>Email<input name="email" type="email" autoComplete="email" required/></label><label>Password<input name="password" type="password" minLength={8} autoComplete="current-password" required/></label><button className="btn btn-primary" formAction={login}>Sign In</button></form></section><section className="auth-card" style={{width:'100%'}}><div className="kpi">New business owner or customer?</div><h2>Create Account</h2><p className="muted small">Choose a username for your account, then create your secure email/password login.</p><form><input type="hidden" name="next" value={next}/><label>Username<input name="username" minLength={3} maxLength={30} pattern="[a-zA-Z0-9][a-zA-Z0-9._-]{2,29}" autoComplete="username" placeholder="yourusername" required/></label><label>Email<input name="email" type="email" autoComplete="email" required/></label><label>Password<input name="password" type="password" minLength={8} autoComplete="new-password" required/></label><p className="small muted">Username: 3–30 characters using letters, numbers, periods, underscores or hyphens. Password: at least 8 characters.</p><button className="btn btn-primary" formAction={signup}>Create Account</button></form></section></div><div className="notice" style={{marginTop:18}}><strong>Business-owner requirement:</strong> a signed-in account with a username is required before a business can be submitted or claimed. Creating an account does not verify a business, change organic rankings or activate Sponsored placement.</div></div></main></SiteShell>}
