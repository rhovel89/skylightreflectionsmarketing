import type{Metadata}from'next';import{SiteShell}from'@/components/SiteShell';import{getLocations}from'@/lib/data';import{getSiteUrl}from'@/lib/site-url';import Link from'next/link';export const dynamic='force-dynamic';export const metadata:Metadata={title:'Central Illinois Locations',description:'Browse published local businesses and professionals by Central Illinois city, town and nearby market.',alternates:{canonical:'/illinois'},openGraph:{type:'website',url:'/illinois',title:'Central Illinois Locations',description:'Browse local businesses by Central Illinois city, town and market.'}};export default async function Page(){const locations=await getLocations(),base=getSiteUrl(),schema={'@context':'https://schema.org','@type':'ItemList',name:'Central Illinois Locations',itemListOrder:'https://schema.org/ItemListUnordered',numberOfItems:locations.length,itemListElement:locations.map((l:any,i:number)=>({'@type':'ListItem',position:i+1,item:{'@type':'Place',name:`${l.name}, ${l.state||'IL'}`,url:`${base}/illinois/${l.slug}`}}))};return <SiteShell><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(schema).replace(/</g,'\\u003c')}}/><main><section className="pagehero"><div className="container"><h1>Central Illinois Locations</h1><p>Browse published home services, attorneys, restaurants and local stores by city, town and nearby market.</p></div></section><section className="section"><div className="container grid grid-4">{locations.map(l=><Link className="card" key={l.id} href={`/illinois/${l.slug}`}><h3>{l.name}</h3><p className="muted">{l.county?`${l.county} County · `:''}{l.state||'IL'}</p></Link>)}</div></section></main></SiteShell>}
