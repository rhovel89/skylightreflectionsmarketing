import type{Metadata}from'next';import{notFound}from'next/navigation';import{SiteShell}from'@/components/SiteShell';import{BusinessCard}from'@/components/BusinessCard';import{SearchForm}from'@/components/SearchForm';import{getBusinesses,getCategories,getLocations,getSeoPage,getGuides}from'@/lib/data';import Link from'next/link';export const dynamic='force-dynamic';export async function generateMetadata({params}:{params:Promise<{city:string}>}):Promise<Metadata>{const{city}=await params;const locations=await getLocations();const loc=locations.find(l=>l.slug===city);if(!loc)return{};const seo=await getSeoPage(loc.name);return{title:seo?.title?{absolute:seo.title}:`Local Businesses in ${loc.name}, IL`,description:seo?.description||`Explore local businesses in ${loc.name}, Illinois.`,robots:{index:Boolean(seo&&seo.reviewed&&seo.index_mode!=='noindex'),follow:true}}}
export default async function Page({params}:{params:Promise<{city:string}>}){const{city}=await params;const locations=await getLocations();const loc=locations.find(l=>l.slug===city);if(!loc)notFound();const[businesses,cats,seo,guides]=await Promise.all([getBusinesses({city,limit:150}),getCategories(),getSeoPage(loc.name),getGuides(120)]);const counts=new Map<string,number>();for(const b of businesses){const seen=new Set<string>();for(const link of (b as any).business_categories??[]){const slug=link.categories?.slug;if(slug&&!seen.has(slug)){counts.set(slug,(counts.get(slug)||0)+1);seen.add(slug)}}}const cityCats=cats.map(c=>({...c,count:counts.get(c.slug)||0})).filter(c=>c.count>0).sort((a,b)=>b.count-a.count||a.name.localeCompare(b.name));const cityGuides=(guides as any[]).filter(g=>g.city===loc.name).slice(0,3);return <SiteShell><main><section className="pagehero"><div className="container"><div className="crumb">Illinois › {loc.name}</div><h1>{seo?.h1||`Local Businesses in ${loc.name}, Illinois`}</h1><p>{seo?.intro||`Explore local services, attorneys, restaurants and stores with active physical locations in ${loc.name}.`}</p><div className="market-stats"><span><strong>{businesses.length}</strong> published businesses</span><span><strong>{cityCats.length}</strong> active categories</span>{loc.county&&<span><strong>{loc.county}</strong> County</span>}</div></div></section><section className="section"><div className="container"><div className="search-panel"><SearchForm categories={cats} locations={locations} defaults={{city}}/></div><div className="section-head"><div><div className="kpi">Explore {loc.name}</div><h2>Browse Local Categories</h2><p className="muted">Categories shown here currently have published business inventory in this market.</p></div></div>{cityCats.length?<div className="grid grid-4">{cityCats.slice(0,12).map(c=><Link className="card category-card" key={c.id} href={`/illinois/${city}/${c.slug}`}><strong>{c.name}</strong><p className="small muted">{c.count} published business{c.count===1?'':'es'}</p></Link>)}</div>:<div className="empty">Published category inventory is still being developed for this market.</div>}{seo?.content&&<div className="card local-copy"><div className="kpi">Local Guide</div><p className="muted">{seo.content}</p></div>}{cityGuides.length>0&&<section className="inline-guide-section"><div className="section-head compact-head"><div><div className="kpi">Local reading</div><h2>Guides for {loc.name}</h2><p className="muted">Practical articles connected to this market.</p></div><Link href={`/guides?city=${encodeURIComponent(loc.name)}`}>All {loc.name} guides →</Link></div><div className="inline-guide-grid">{cityGuides.map((g:any)=><Link className="inline-guide-card" key={g.id} href={`/guides/${g.slug}`}><span>{g.type==='local_guide'?'City & Category Guide':g.type||'Local Guide'}</span><strong>{g.title}</strong><p>{g.summary}</p></Link>)}</div></section>}<div className="section-head business-heading"><div><h2>Businesses in {loc.name}</h2><p className="muted">Canonical business profiles with active physical-location evidence in this market.</p></div><Link href={`/search?city=${encodeURIComponent(city)}`}>Search {loc.name} →</Link></div>{businesses.length?<div className="business-list">{businesses.map(b=><BusinessCard key={b.id} business={b}/>)}</div>:<div className="empty">No published physical-location inventory is available for this market yet.</div>}</div></section></main></SiteShell>}
